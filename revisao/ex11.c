#include <stdio.h>

int main() {
    // Imagine que você está criando um sistema que precisa copiar um nome digitado
    // pelo usuário de uma variável para outra, para uso posterior em um relatório.
    // Qual função da biblioteca <string.h> realiza essa cópia corretamente?
    // a) copy(nomeDestino, nomeOrigem);
    // b) strcpy(nomeDestino, nomeOrigem);
    // c) strcmp(nomeDestino, nomeOrigem);
    // d) strcat(nomeDestino, nomeOrigem);
    // e) assign(nomeDestino, nomeOrigem);

    return 0;
}