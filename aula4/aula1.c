#include <stdio.h>

int main() {
    float n1, n2, med; 
    printf("Num1: ");
    scanf("%f", &n1);
    printf("Num2: ");
    scanf("%f", &n2);
    med = (n1+n2)/2;
    printf("Média: %f", med);

    return 0;
}
